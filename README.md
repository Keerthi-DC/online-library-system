# Online Library System (React Assignment 2)

## Project Overview

This project is an Online Library System developed using React and Vite. It is a single-page application that allows users to browse books, search for books, view book details, and add new books to the library.

The project was built to demonstrate React concepts such as routing, reusable components, Redux Toolkit for state management, forms, and responsive UI design.

## Features

* Home page with a welcome message
* Book categories displayed on the home page
* Popular books section
* Browse Books page with category-based filtering
* Search books by title
* Book Details page using dynamic routing
* Add Book page with form validation
* Redux Toolkit for managing book data
* Custom 404 page for invalid routes
* Responsive design that works on different screen sizes

## Technologies Used

* React
* Vite
* React Router DOM
* Redux Toolkit
* CSS

## Prerequisites

Before running this project, make sure you have:

* Node.js (version 20 or above)
* npm
* Git (optional)

## Installation

Clone the repository:

```bash
git clone https://github.com/your-username/online-library-system.git
```

Go to the project folder:

```bash
cd online-library
```

Install the required packages:

```bash
npm install
```

Start the development server:

```bash
npm run dev
```

The application will open in your browser at:

```
http://localhost:5173
```

(If port 5173 is already in use, Vite will automatically use another available port.)

## Project Structure

```
online-library/
│
├── src/
│   ├── components/
│   ├── pages/
│   ├── routes/
│   ├── store/
│   ├── data/
│   └── index.css
│
├── public/
├── package.json
├── vite.config.js
└── README.md
```

## Assignment Requirements Covered

* Project created using Vite
* Home page with welcome section and categories
* Browse Books page
* Search functionality
* Dynamic routing for book details
* Add Book form with validation
* Redux Toolkit for state management
* 404 Not Found page
* Responsive design
* Reusable React components

## Submission Instructions

* Remove the **node_modules** folder before creating the ZIP file.
* Upload the project to GitHub.
* Include the GitHub repository link in the submission.
* Make sure the project runs correctly using:

```bash
npm run dev
```
